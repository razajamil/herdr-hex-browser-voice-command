{
  "name": "voice-router",
  "version": "0.1.0",
  "private": true,
  "description": "Routes Hex voice transcripts to the right herdr Claude Code pane based on the active browser tab.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p daemon/tsconfig.json && tsc -p extension/tsconfig.json",
    "check": "npm run typecheck && npm run build",
    "test:match": "node daemon/dist/test-match.js"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.5",
    "@hono/standard-validator": "^0.2.2",
    "hono": "^4.12.26",
    "hono-openapi": "^1.3.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "@types/node": "^26.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  }
}
